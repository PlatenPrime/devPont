{
    "name": "@devPont",
    "private": true,
    "version": "0.0.0", 

        "scripts": {
          "b": "npm --filter @devPont/backend",
          "w": "npm --filter @devPont/webapp",
          "dev": "npm -r --parallel dev",
          "types": "concurrently 'npm b types' 'npm w types'",
          "prettify": "npm -r --parallel prettify"
        },
        "devDependencies": {
          "concurrently": "^8.2.0",
          "prettier": "^3.0.2",
          "rimraf": "^5.0.1",
          "ts-node": "^10.9.1",
          "ts-node-dev": "^2.0.0",
          "typescript": "^5.1.6"
        }

}